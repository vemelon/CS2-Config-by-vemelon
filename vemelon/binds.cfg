
//----Controlling----
bind mouse3 +reload		// reload 
bind mouse4 slot6 		// grab HE 
bind mouse5 slot7 		// grab flashbang
bind r slot8 			// grab smokegrenade
bind f slot10 			// grab molotov
bind t slot9  			// grab decoy
bind q "player_ping" 	// ping / mark
bind tab "+showscores"  // Scoretab
bind p "toggle volume 1 0.5 0.3 0.1 0.05 0.03" // changes master volume 
bind j "incrementvar cl_radar_scale 0.2 1 0.1" // changes zoom of radar on the top left
bind l "incrementvar hud_scaling 0.5 0.95 0.05" // changes size of the HUD incl the Radar
bind h "+radialradio3" 	// Command-wheel3

bind mwheelup "slot2"
bind mwheeldown "slot1"

// bind i "crosshairset"

bind v "+voicerecord" 	// talking ingame
bind k "+lookatweapon" 	// skin showcase animation
bind alt "+buyswitch" 	// alternate commands on hold

// bind capslock "bhop" // activates jumping on mousewheel, press again to deactivate

bind scancode29 messagemode // binds y on a non-US keyboard to Chat
bind scancode28 "radioswitch" // binds z on a non-US keyboard to ...

//-----ViewModel Fastswitch-----
bind rightarrow "toggle viewmodel_offset_x -2 -1.5 -1 -0.5 0 0.5 1 1.5 2 2.5"
bind uparrow "toggle viewmodel_offset_y -2 -1.5 -1 -0.5 0 0.5 1 1.5 2"
bind downarrow "toggle viewmodel_offset_z 2 1.5 1 0.5 0 -0.5 -1 -1.5 -2"
bind leftarrow "toggle viewmodel_fov 54 56 58 60 62 64 66 68" 

//-----F Keys------
bind f5 "say ♿"
bind f6 "say 卐"
// bind f7 "say Try your best to do those things. Play a lot of DM. Aim for the head. Be efficient. You will rank up. Also, don't care about smurfs."
// bind f8 "say If you can beat them, you're getting better. You improve way faster playing with better players than you."
// bind f9 "say JAB JAB FORWARD JAB FIERCE"
// bind f10 "ignorerad" // block/ignore radio commands
// bind f11 ""
bind f12 "toggleconsole"

//----Chat Wheels---
bind "capslock" "+radialradio"
// bind "" "+radialradio2"
// bind "" "+radialradio3"



//----BuyScript----
bind f1 "buy ak47; buy m4a1_silencer"
bind f2 "buy vesthelm; buy defuser"
bind f3 "buy hegrenade; buy incgrenade; buy smokegrenade; buy molotov; buy flashbang"
bind f4 "buy p250"